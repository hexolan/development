apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: auth-service
  name: auth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: auth-service
  strategy: {}
  template:
    metadata:
      labels:
        io.kompose.network/docker-stocklet-auth-network: "true"
        io.kompose.network/docker-stocklet-network: "true"
        io.kompose.service: auth-service
    spec:
      containers:
        - image: stocklet/auth-service
          name: auth-service
          ports:
            - containerPort: 90
              protocol: TCP
            - containerPort: 9090
              protocol: TCP
          resources: {}
      restartPolicy: Always
status: {}
