apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: order-service
  name: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: order-service
  strategy: {}
  template:
    metadata:
      labels:
        io.kompose.network/docker-stocklet-network: "true"
        io.kompose.network/docker-stocklet-order-network: "true"
        io.kompose.service: order-service
    spec:
      containers:
        - image: stocklet/order-service
          name: order-service
          ports:
            - containerPort: 90
              protocol: TCP
            - containerPort: 9090
              protocol: TCP
          resources: {}
      restartPolicy: Always
status: {}
