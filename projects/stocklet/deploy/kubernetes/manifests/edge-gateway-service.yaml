apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: edge-gateway
  name: edge-gateway
spec:
  ports:
    - name: "80"
      port: 80
      targetPort: 80
  selector:
    io.kompose.service: edge-gateway
status:
  loadBalancer: {}
