apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: user-service
  name: user-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: user-service
  strategy: {}
  template:
    metadata:
      labels:
        io.kompose.network/docker-stocklet-network: "true"
        io.kompose.network/docker-stocklet-user-network: "true"
        io.kompose.service: user-service
    spec:
      containers:
        - image: stocklet/user-service
          name: user-service
          ports:
            - containerPort: 90
              protocol: TCP
            - containerPort: 9090
              protocol: TCP
          resources: {}
      restartPolicy: Always
status: {}
